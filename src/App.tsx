import { Container } from '@mantine/core';

function App() {
    return (
        <Container>
            <h1>Hello, world!</h1>
        </Container>
    );
}

// eslint-disable-next-line import/no-default-export
export default App;
